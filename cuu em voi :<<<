thầy ơi :<
nếu được thì thầy xóa hộ e cái phần 
int PerfectNumber(int n){
    int sum = 0;
    for (int i=1;i<=n/2; i++) {
        if (n%i== 0) {
            sum += i;
        }
    }
    return (sum==n&&n!= 0);
này với cái ở case 3:
count=0;
                for (int i=0;i<n;i++) {
                    if (PerfectNumber(arr[i])) {
                        count++;
                    }
                }
                printf("So luong so hoan hao trong mang la: %d\n", count);
                break;
được không thầy :<< với print thêm "\n11. Thoat" ở phần print dài được không ạ :((( tại cái rikkeacademy của e mấy cái bài đọc bị lỗi nên em không thể đọc được bài trước và trong khi kiểm tra ạ @@ e hỏi 2 thầy chính 2 3 thầy trợ giảng nhưng cái tình trạng này vẫn diễn ra nên em vào bài của
ông bạn em đã đưa trước để copy cái case 3 nhưng lại k được ạ :(((((
nếu được thì em cảm ơn ạ
